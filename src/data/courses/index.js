import { demoCourse, demoCourseMap } from './demo-course.js';
import { workCourse, workCourseMap } from './work-course.js';

export const courses = [
  {
    course: demoCourse,
    courseMap: demoCourseMap,
  },
  {
    course: workCourse,
    courseMap: workCourseMap,
  },
];

export { demoCourse, demoCourseMap, workCourse, workCourseMap };